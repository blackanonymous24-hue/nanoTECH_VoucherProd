# Copier vers deploy/vps.target.local.env (ignoré par Git) :
#   copy deploy\vps.target.local.env.example deploy\vps.target.local.env
#
# VPS de DESTINATION (nouveau serveur).

TARGET_VPS_HOST=62.171.173.190
TARGET_VPS_USER=root
TARGET_VPS_PORT=22
TARGET_VPS_SSH_PASSWORD=Maman4155

TARGET_VPS_APP_DIR=/var/www/vouchernet

# Optionnel — domaine pour Nginx / HTTPS (ex. nanovoucher.com)
# TARGET_DOMAIN=nanovoucher.com

# Branche Git à cloner si le dossier app n'existe pas encore sur le nouveau VPS
VOUCHERNET_BRANCH=main
GITHUB_REPO=https://github.com/blackanonymous24-hue/nanoTECH_VoucherProd1.git
