# Copier vers deploy/vps.local.env (ignoré par Git) :
#   copy deploy\vps.local.env.example deploy\vps.local.env
#
# À remplir : uniquement VPS_SSH_PASSWORD (le reste est préconfiguré).

VPS_HOST=69.62.110.53
VPS_USER=root
VPS_PORT=22
VPS_SSH_PASSWORD=

VPS_APP_DIR=/var/www/vouchernet
VOUCHERNET_BRANCH=main
