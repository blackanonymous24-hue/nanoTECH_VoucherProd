auto-install-peers=false
strict-peer-dependencies=false
# Flat node_modules required by Metro/React Native bundler and EAS build toolchain.
# Without hoisting, Metro cannot traverse symlinked packages and native linking fails.
node-linker=hoisted
