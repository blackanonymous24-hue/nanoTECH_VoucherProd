openapi: "3.1.0"
info:
  title: VoucherNet API
  version: "1.0.0"
servers:
  - url: /api

paths:
  /healthz:
    get:
      operationId: getHealth
      summary: Health check
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /routers:
    get:
      operationId: listRouters
      summary: List all routers
      responses:
        "200":
          description: List of routers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Router"
    post:
      operationId: createRouter
      summary: Add a router
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateRouterBody"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Router"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}:
    get:
      operationId: getRouter
      summary: Get a router
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Router"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      operationId: updateRouter
      summary: Update a router
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateRouterBody"
      responses:
        "200":
          description: Updated router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Router"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteRouter
      summary: Delete a router
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/test:
    post:
      operationId: testRouterConnection
      summary: Test connection to a router
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Test result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TestResult"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/profiles:
    get:
      operationId: listRouterProfiles
      summary: List hotspot profiles from a router
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of hotspot profiles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HotspotProfile"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Cannot reach router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/profiles/{profileName}:
    put:
      operationId: updateRouterProfile
      summary: Update a hotspot user profile on the router
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
        - in: path
          name: profileName
          required: true
          schema:
            type: string
          description: Original profile name (URL-encoded). When `mikrotikId` is sent, it must still match that row on the router.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateHotspotProfileRequest"
      responses:
        "200":
          description: Profile updated
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok:
                    type: boolean
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Cannot reach router or MikroTik error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/sessions:
    get:
      operationId: listRouterSessions
      summary: List active hotspot sessions
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: List of active sessions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/HotspotSession"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Cannot reach router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/users:
    get:
      operationId: listRouterUsers
      summary: List hotspot users from a router (paginated, server-side search)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
        - in: query
          name: search
          schema:
            type: ["string", "null"]
        - in: query
          name: profile
          schema:
            type: ["string", "null"]
        - in: query
          name: limit
          schema:
            type: ["integer", "null"]
        - in: query
          name: offset
          schema:
            type: ["integer", "null"]
      responses:
        "200":
          description: Paginated list of hotspot users
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HotspotUserListResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Cannot reach router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/sales:
    get:
      operationId: getRouterSales
      summary: Fetch live sales report from MikroTik scripts (MikHmon-compatible)
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Sales report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SalesReport"
        "502":
          description: Cannot reach router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/logs:
    get:
      operationId: listRouterLogs
      summary: Fetch system log from MikroTik router
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
        - in: query
          name: limit
          schema:
            type: integer
        - in: query
          name: topics
          schema:
            type: string
        - in: query
          name: live
          description: When "1", bypass short-lived server cache for this request
          schema:
            type: string
        - in: query
          name: hotspotUsers
          description: When "1", return only hotspot topic lines about user sessions (login/logout/failures)
          schema:
            type: string
      responses:
        "200":
          description: Log entries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/LogEntry"
        "502":
          description: Cannot reach router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/sessions/disconnect:
    post:
      operationId: disconnectRouterSession
      summary: Disconnect an active hotspot session
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DisconnectSessionBody"
      responses:
        "200":
          description: Disconnect result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DisconnectResult"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Cannot reach router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/sync:
    post:
      operationId: syncRouterVouchers
      summary: Import hotspot users from MikroTik into local DB
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Sync result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SyncResult"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Cannot reach router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /routers/{id}/sync-usage:
    post:
      operationId: syncVoucherUsage
      summary: Sync voucher usage from MikroTik logs and MikHmon scripts
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Sync result
          content:
            application/json:
              schema:
                type: object
                required: [updated, total, usedOnRouter]
                properties:
                  updated:
                    type: integer
                  total:
                    type: integer
                  usedOnRouter:
                    type: integer
                  message:
                    type: string
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Cannot reach router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /vendors:
    get:
      operationId: listVendors
      summary: List all vendors
      responses:
        "200":
          description: List of vendors
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Vendor"
    post:
      operationId: createVendor
      summary: Create a vendor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateVendorBody"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vendor"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /vendors/{id}:
    put:
      operationId: updateVendor
      summary: Update a vendor
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateVendorBody"
      responses:
        "200":
          description: Updated vendor
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vendor"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteVendor
      summary: Delete a vendor
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /vendors/{id}/report:
    get:
      operationId: getVendorReport
      summary: Get sales report for a vendor
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Vendor report
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VendorReport"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /vendors/reports/summary:
    get:
      operationId: getVendorReportsSummary
      summary: Get summary sales stats for all vendors
      responses:
        "200":
          description: Vendor summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/VendorSummary"

  /vouchers:
    get:
      operationId: listVouchers
      summary: List generated vouchers
      parameters:
        - in: query
          name: routerId
          schema:
            type: ["integer", "null"]
        - in: query
          name: profile
          schema:
            type: ["string", "null"]
        - in: query
          name: printed
          schema:
            type: ["string", "null"]
            enum: ["true", "false", null]
        - in: query
          name: limit
          schema:
            type: ["integer", "null"]
        - in: query
          name: offset
          schema:
            type: ["integer", "null"]
      responses:
        "200":
          description: List of vouchers
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoucherListResponse"

  /vouchers/generate:
    post:
      operationId: generateVouchers
      summary: Generate vouchers on MikroTik
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateVouchersBody"
      responses:
        "201":
          description: Generated vouchers
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Voucher"
        "400":
          description: Bad request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "502":
          description: Cannot reach router
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /vouchers/{id}:
    delete:
      operationId: deleteVoucher
      summary: Delete a voucher
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /vouchers/{id}/mark-printed:
    post:
      operationId: markVoucherPrinted
      summary: Mark a voucher as printed
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Updated voucher
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Voucher"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /dashboard:
    get:
      operationId: getDashboard
      summary: Get dashboard statistics
      responses:
        "200":
          description: Dashboard stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardStats"

components:
  schemas:
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string

    Router:
      type: object
      required: [id, name, host, port, username, isActive, createdAt, updatedAt]
      properties:
        id:
          type: integer
        name:
          type: string
        host:
          type: string
        port:
          type: integer
        username:
          type: string
        isActive:
          type: boolean
        currency:
          type: string
          description: Libellé monnaie affiché (ex. FCFA, EUR)
          default: FCFA
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateRouterBody:
      type: object
      required: [name, host, username, password]
      properties:
        name:
          type: string
        host:
          type: string
        port:
          type: integer
          default: 8728
        username:
          type: string
        password:
          type: string
        isActive:
          type: boolean
          default: true
        currency:
          type: string
          description: Libellé monnaie (tickets, rapports)
          default: FCFA

    UpdateRouterBody:
      type: object
      properties:
        name:
          type: string
        host:
          type: string
        port:
          type: integer
        username:
          type: string
        password:
          type: string
        isActive:
          type: boolean
        currency:
          type: string
          description: Libellé monnaie (tickets, rapports)

    TestResult:
      type: object
      required: [success, message]
      properties:
        success:
          type: boolean
        message:
          type: string
        routerBoard:
          type: ["string", "null"]
        version:
          type: ["string", "null"]

    HotspotProfile:
      type: object
      required: [name, schedulerMonitorActive]
      properties:
        name:
          type: string
        mikrotikId:
          type: string
          description: Internal RouterOS id of the profile row
        rateLimit:
          type: ["string", "null"]
        validity:
          type: ["string", "null"]
        price:
          type: ["string", "null"]
        sellingPrice:
          type: ["string", "null"]
        sharedUsers:
          type: ["string", "null"]
        addrPool:
          type: ["string", "null"]
        lockMac:
          type: boolean
        expiredMode:
          type: ["string", "null"]
        parentQueue:
          type: ["string", "null"]
        schedulerMonitorActive:
          type: boolean
          description: True when a /system scheduler entry exists for this profile name and is enabled (Mikhmon-style monitor).

    UpdateHotspotProfileRequest:
      type: object
      required: [name, price, validity]
      properties:
        name:
          type: string
        validity:
          type: string
        price:
          type: string
        sellingPrice:
          type: string
        sharedUsers:
          type: string
        addrPool:
          type: string
        rateLimit:
          type: string
        expiredMode:
          type: string
        lockMac:
          type: boolean
        parentQueue:
          type: string
        mikrotikId:
          type: string
          description: >
            Optional internal RouterOS `.id` of the profile (same as `mikrotikId` from list profiles).
            When valid and matching the path profile name, the server resolves the row with a single
            `print ?.id=` instead of `print ?name=`, which is faster on routers with many profiles.

    HotspotSession:
      type: object
      required: [user, address, uptime]
      properties:
        user:
          type: string
        address:
          type: string
        macAddress:
          type: ["string", "null"]
        uptime:
          type: string
        bytesIn:
          type: ["string", "null"]
        bytesOut:
          type: ["string", "null"]
        server:
          type: ["string", "null"]

    Voucher:
      type: object
      required: [id, routerId, username, password, profileName, price, validity, createdAt]
      properties:
        id:
          type: integer
        routerId:
          type: integer
        username:
          type: string
        password:
          type: string
        profileName:
          type: string
        price:
          type: string
        validity:
          type: string
        comment:
          type: ["string", "null"]
        printedAt:
          type: ["string", "null"]
          format: date-time
        usedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time

    VoucherListResponse:
      type: object
      required: [vouchers, total]
      properties:
        vouchers:
          type: array
          items:
            $ref: "#/components/schemas/Voucher"
        total:
          type: integer

    Vendor:
      type: object
      required: [id, name, isActive, createdAt, updatedAt]
      properties:
        id:
          type: integer
        name:
          type: string
        phone:
          type: ["string", "null"]
        isActive:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CreateVendorBody:
      type: object
      required: [name]
      properties:
        name:
          type: string
        phone:
          type: ["string", "null"]

    UpdateVendorBody:
      type: object
      properties:
        name:
          type: string
        phone:
          type: ["string", "null"]
        isActive:
          type: boolean

    VendorProfileStat:
      type: object
      required: [profileName, total, printed]
      properties:
        profileName:
          type: string
        total:
          type: integer
        printed:
          type: integer

    VendorReport:
      type: object
      required: [vendor, totalVouchers, totalPrinted, totalUsed, byProfile, recentVouchers, salesStats]
      properties:
        vendor:
          $ref: "#/components/schemas/Vendor"
        totalVouchers:
          type: integer
        totalPrinted:
          type: integer
        totalUsed:
          type: integer
        salesStats:
          $ref: "#/components/schemas/VendorSalesStats"
        byProfile:
          type: array
          items:
            $ref: "#/components/schemas/VendorProfileStat"
        recentVouchers:
          type: array
          items:
            $ref: "#/components/schemas/Voucher"

    VendorSalesStats:
      type: object
      required: [todaySold, todayAmount, yesterdaySold, yesterdayAmount, weekSold, weekAmount, lastWeekSold, lastWeekAmount, thisMonthSold, thisMonthAmount, lastMonthSold, lastMonthAmount]
      properties:
        todaySold:
          type: integer
        todayAmount:
          type: number
        yesterdaySold:
          type: integer
        yesterdayAmount:
          type: number
        weekSold:
          type: integer
        weekAmount:
          type: number
        lastWeekSold:
          type: integer
        lastWeekAmount:
          type: number
        thisMonthSold:
          type: integer
        thisMonthAmount:
          type: number
        lastMonthSold:
          type: integer
        lastMonthAmount:
          type: number

    VendorSummary:
      type: object
      required: [vendor, totalVouchers, totalPrinted, totalUsed, salesStats]
      properties:
        vendor:
          $ref: "#/components/schemas/Vendor"
        totalVouchers:
          type: integer
        totalPrinted:
          type: integer
        totalUsed:
          type: integer
        salesStats:
          $ref: "#/components/schemas/VendorSalesStats"

    GenerateVouchersBody:
      type: object
      required: [routerId, profile, qty]
      properties:
        routerId:
          type: integer
        profile:
          type: string
        qty:
          type: integer
          minimum: 1
          maximum: 200
        prefix:
          type: ["string", "null"]
        comment:
          type: ["string", "null"]
        server:
          type: ["string", "null"]
        vendorId:
          type: ["integer", "null"]
        passwordMode:
          type: string
          enum: [same, random]

    SalesReport:
      type: object
      required: [dailyCount, dailyAmount, monthlyCount, monthlyAmount, dateLabel, monthLabel]
      properties:
        dailyCount:
          type: integer
        dailyAmount:
          type: number
        monthlyCount:
          type: integer
        monthlyAmount:
          type: number
        dateLabel:
          type: string
        monthLabel:
          type: string

    LogEntry:
      type: object
      required: [id, time, topics, message]
      properties:
        id:
          type: string
        time:
          type: string
        topics:
          type: string
        message:
          type: string

    DisconnectSessionBody:
      type: object
      required: [user]
      properties:
        user:
          type: string

    DisconnectResult:
      type: object
      required: [removed, user]
      properties:
        removed:
          type: integer
        user:
          type: string

    HotspotUserListResponse:
      type: object
      required: [users, total]
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/HotspotUser"
        total:
          type: integer

    HotspotUser:
      type: object
      required: [username, password, profile, disabled]
      properties:
        username:
          type: string
        password:
          type: string
        profile:
          type: string
        comment:
          type: ["string", "null"]
        limitUptime:
          type: ["string", "null"]
        limitBytesTotal:
          type: ["string", "null"]
        macAddress:
          type: ["string", "null"]
        server:
          type: ["string", "null"]
        disabled:
          type: boolean

    SyncResult:
      type: object
      required: [imported, skipped, total]
      properties:
        imported:
          type: integer
        skipped:
          type: integer
        total:
          type: integer

    DashboardStats:
      type: object
      required: [totalVouchers, routerCount, dailySalesCount, dailySalesAmount, monthlySalesCount, monthlySalesAmount]
      properties:
        totalVouchers:
          type: integer
        routerCount:
          type: integer
        dailySalesCount:
          type: integer
        dailySalesAmount:
          type: number
        monthlySalesCount:
          type: integer
        monthlySalesAmount:
          type: number
        recentVouchers:
          type: array
          items:
            $ref: "#/components/schemas/Voucher"
